# 技术文档

## 架构设计

### 系统架构

NewsFlow 采用 **MCP (Model Context Protocol) 服务架构**，通过 Cursor AI 助手调用 MCP 服务完成任务。

```
┌─────────────────────────────────────────────────────────┐
│                    Cursor AI 助手                        │
│  (通过对话调用MCP服务，协调整个流程)                       │
│  - 读取链接，判断是否为文章链接                           │
│  - 如果是文章链接，生成一两百字总结                         │
└────────────────────┬────────────────────────────────────┘
                     │ MCP Protocol
                     │
                     ▼
        ┌──────────────────┐
        │  MCP服务1        │
        │  URL提取服务     │
        │                  │
        │  - Selenium访问  │
        │  - 等待页面渲染  │
        │  - 提取a标签     │
        │  - 转为绝对路径  │
        └──────────────────┘
                     │
                     │ (链接列表)
                     ▼
        ┌──────────────────┐
        │  Cursor AI       │
        │  - 判断文章链接   │
        │  - 生成一两百字总结 │
        └──────────────────┘
                     │
                     │ (文章标题、总结、URL)
                     ▼
        ┌──────────────────┐
        │  MCP服务2        │
        │  Markdown写入服务│
        │                  │
        │  - 创建日期文件夹 │
        │  - 保存MD文件    │
        │  - 文件名处理    │
        └──────────────────┘
                     │
                     ▼
        ┌──────────────────┐
        │   输出目录       │
        │  output/         │
        │  ├── 2024-01-01/ │
        │  │   ├── 文章1.md│
        │  │   └── 文章2.md│
        │  └── ...         │
        └──────────────────┘
```

**架构特点**：
1. **MCP服务架构**：两个MCP服务，职责单一
2. **Cursor AI协调**：Cursor负责AI任务（链接判断、文章总结）
3. **Python运行环境**：MCP服务使用Python实现
4. **无需外部AI服务**：只需要Cursor，无需OpenAI/Claude API

### MCP服务拆分

**服务1：URL提取服务 (newsflow-extract)**
- **功能**：访问网站，提取所有链接（支持CSR网站）
- **工具**：
  - `extract_links_from_url(url: str) -> List[str]`: 
    - 访问URL，等待页面完全渲染（处理CSR网站）
    - 提取所有a标签
    - 将相对路径转为绝对路径
    - 返回链接列表

**服务2：Markdown写入服务 (newsflow-writer)**
- **功能**：创建日期文件夹，保存Markdown文件，发送邮件通知
- **工具**：
  - `save_markdown_file(title: str, original_title: str, summary: str, url: str, date: str = None) -> dict`:
    - 创建日期文件夹（YYYY-MM-DD格式）
    - 文件名使用文章标题（安全处理）
    - 按照需求文档格式保存Markdown
  - `create_date_folder(date: str = None) -> str`: 创建日期文件夹
  - `send_email_from_date_folder(date: str, recipient_email: str, subject: str = None) -> dict`:
    - 读取指定日期文件夹中的所有Markdown文件
    - 将所有文章内容整理成邮件正文
    - 发送邮件到指定邮箱地址

**资源（Resources）**：
- `config`: 读取配置文件（可选）

### 执行流程

**通过Cursor AI助手交互式执行：**

```
1. 用户通过Cursor对话：启动NewsFlow任务
   └─> Cursor读取配置文件（可选）

2. Cursor调用URL提取服务处理每个网站：
   ├─> 调用 newsflow-extract::extract_links_from_url(url)
   ├─> MCP服务：使用Selenium Chrome WebDriver访问URL
   ├─> MCP服务：等待页面完全渲染（driver.implicitly_wait(5)）
   ├─> MCP服务：获取渲染后的HTML（driver.page_source）
   ├─> MCP服务：使用BeautifulSoup解析HTML
   ├─> MCP服务：提取所有a标签，转为绝对路径
   └─> 返回链接列表给Cursor

3. Cursor处理每个链接：
   ├─> Cursor读取链接内容（直接访问URL）
   ├─> Cursor使用AI判断：是否为文章链接？
   ├─> 如果是文章链接：
   │   ├─> Cursor读取文章内容
   │   └─> Cursor生成一两百字总结
   └─> 如果不是文章链接：跳过

4. Cursor调用Markdown写入服务保存文章：
   ├─> 调用 newsflow-writer::save_markdown_file(
   │       title="文件名", 
   │       original_title="原标题",
   │       summary="一两百字总结",
   │       url="原文链接",
   │       date="2024-01-01"  # 可选，默认今天
   │   )
   ├─> MCP服务：创建日期文件夹（如果不存在）
   ├─> MCP服务：文件名安全处理
   └─> MCP服务：按照格式保存Markdown文件

5. （可选）Cursor调用邮件发送服务发送邮件：
   ├─> 调用 newsflow-writer::send_email_from_date_folder(
   │       date="2024-01-01",
   │       recipient_email="recipient@example.com",
   │       subject="NewsFlow - 2024-01-01 新闻摘要"  # 可选
   │   )
   ├─> MCP服务：读取指定日期文件夹中的所有.md文件
   ├─> MCP服务：解析每个Markdown文件，提取文章信息
   ├─> MCP服务：生成HTML格式的邮件正文
   └─> MCP服务：通过SMTP发送邮件

6. 完成后，Cursor输出统计信息
```

**关键点**：
- **MCP服务1**：使用Selenium Chrome WebDriver访问网站，等待页面完全渲染后提取链接（支持CSR网站）
- **Cursor负责**：
  - 读取链接内容（直接访问URL）
  - 判断是否为文章链接
  - 生成一两百字总结
- **MCP服务2**：只负责文件保存，按照需求文档的格式
- **交互式执行**：通过对话方式执行，可以随时调整和优化

### 技术选型

#### MCP协议
- **MCP SDK**：`mcp` (Python MCP服务器框架)
- **协议版本**：Model Context Protocol (MCP)

#### 编程语言
- **Python 3.8+**（用于实现MCP服务）

#### 核心依赖库
- **MCP框架**：`mcp` 或 `@modelcontextprotocol/sdk-python`
- **浏览器渲染（CSR支持）**：`selenium`（使用Chrome WebDriver无头模式）
- **ChromeDriver管理**：`webdriver-manager`（自动管理ChromeDriver，无需手动安装）
- **HTML解析**：`BeautifulSoup4`
- **URL处理**：`urllib.parse` (Python内置)
- **配置管理**：`pyyaml`
- **日志记录**：`logging` (Python内置)
- **邮件发送**：`smtplib` (Python内置，用于SMTP邮件发送)
- **邮件内容构建**：`email.mime.text`, `email.mime.multipart` (Python内置，用于构建HTML邮件)

#### AI能力
- **由Cursor提供**：
  - 链接判断：判断链接是否为文章链接
  - 文章总结：生成一两百字总结
- **无需额外AI服务**：不需要OpenAI/Claude API

#### Cursor集成
- **通过MCP连接**：在Cursor中配置MCP服务器
- **工具调用**：Cursor调用MCP服务提供的工具
- **直接访问URL**：Cursor可以直接访问文章URL获取内容（无需MCP服务）

## MCP服务设计

### MCP服务1：URL提取服务 (newsflow-extract)

**服务名称**：`newsflow-extract`  
**功能**：从网站提取所有链接（支持CSR网站）

**工具 (Tools)**：
1. **extract_links_from_url**
   ```python
   def extract_links_from_url(url: str) -> dict:
       """
       从指定URL提取所有a标签链接
       
       特点：
       - 支持CSR网站（等待页面完全渲染）
       - 自动将相对路径转为绝对路径
       - 提取所有a标签的href属性
       
       参数:
         url: 网站URL（如 "https://example.com"）
       
       返回:
         {
           "links": [
             "https://example.com/article1",
             "https://example.com/article2",
             ...
           ],
           "count": 链接数量
         }
       
       实现要点：
       1. 使用Selenium Chrome WebDriver无头模式访问URL：
          ```python
          from selenium import webdriver
          from selenium.webdriver.chrome.options import Options
          from webdriver_manager.chrome import ChromeDriverManager
          
          options = Options()
          options.add_argument("--headless")  # 无头模式，不显示浏览器窗口
          options.add_argument("--disable-gpu")
          
          driver = webdriver.Chrome(
              ChromeDriverManager().install(), 
              options=options
          )
          driver.get(url)
          ```
       2. 等待页面完全渲染：
          ```python
          driver.implicitly_wait(5)  # 隐式等待5秒
          ```
       3. 获取渲染后的HTML：
          ```python
          html = driver.page_source
          ```
       4. 关闭浏览器：
          ```python
          driver.quit()
          ```
       5. 使用BeautifulSoup解析HTML：
          ```python
          from bs4 import BeautifulSoup
          soup = BeautifulSoup(html, 'html.parser')
          ```
       6. 提取所有<a>标签的href属性
       7. 使用urllib.parse.urljoin转为绝对路径
       8. 过滤掉javascript:、mailto:、#等特殊链接
       9. 去重链接列表
       
       优势：
       - 无头模式运行，不打开浏览器窗口，性能更好
       - webdriver-manager自动管理ChromeDriver，无需手动安装
       - 支持CSR网站，可以获取JavaScript渲染后的内容
       """
   ```

**资源 (Resources)**：无

---

### MCP服务2：Markdown写入服务 (newsflow-writer)

**服务名称**：`newsflow-writer`  
**功能**：创建日期文件夹，保存Markdown文件（按照需求文档格式）

**工具 (Tools)**：
1. **save_markdown_file**
   ```python
   def save_markdown_file(
       title: str,
       original_title: str,
       summary: str,
       url: str,
       date: str = None
   ) -> dict:
       """
       保存Markdown文件到日期文件夹
       
       按照需求文档格式保存：
       ```markdown
       **原名**
       文章原标题
       
       ***ai总结内容**
       AI生成的总结内容
       
       ***原文链接**
       文章原始URL
       ```
       
       参数:
         title: 文件名（文章标题，需要安全处理）
         original_title: 文章原标题（Markdown中的"原名"）
         summary: AI生成的一两百字总结（Markdown中的"ai总结内容"）
         url: 文章原始URL（Markdown中的"原文链接"）
         date: 日期（YYYY-MM-DD），默认为今天
       
       返回:
         {
           "path": "保存路径（完整路径）",
           "filename": "文件名.md",
           "date_folder": "日期文件夹路径",
           "success": True/False
         }
       
       实现要点：
       1. 如果date为None，使用今天的日期（YYYY-MM-DD）
       2. 创建日期文件夹（如果不存在）：output/YYYY-MM-DD/
       3. 文件名安全处理（去除特殊字符）
       4. 如果文件已存在，覆盖保存
       5. 按照需求文档的格式写入Markdown内容
       ```
   ```

2. **create_date_folder**
   ```python
   def create_date_folder(date: str = None) -> str:
       """
       创建日期文件夹
       
       参数:
         date: 日期（YYYY-MM-DD），默认为今天
       
       返回:
         文件夹完整路径（如："./output/2024-01-01"）
       ```
   ```

3. **send_email_from_date_folder**
   ```python
   def send_email_from_date_folder(
       date: str,
       recipient_email: str,
       subject: str = None,
       config: dict = None
   ) -> dict:
       """
       读取指定日期文件夹中的所有Markdown文件，并发送邮件到指定邮箱
       
       功能：
       - 读取指定日期文件夹（YYYY-MM-DD格式）下的所有.md文件
       - 解析每个Markdown文件，提取文章标题、总结和链接
       - 将所有文章内容整理成格式化的邮件正文（HTML格式）
       - 通过SMTP服务器发送邮件到指定邮箱
       
       邮件格式：
       - 主题：默认格式为"NewsFlow - YYYY-MM-DD 新闻摘要"，或使用自定义主题
       - 正文：HTML格式，包含：
         * 日期标题
         * 文章列表（每个文章包含：原名、AI总结、原文链接）
         * 文章总数统计
       
       参数:
         date: 日期（YYYY-MM-DD格式），要读取的日期文件夹
         recipient_email: 收件人邮箱地址
         subject: 邮件主题（可选），默认为"NewsFlow - {date} 新闻摘要"
         config: 配置字典（可选），包含SMTP服务器配置
       
       返回:
         {
           "success": True/False,
           "date": "日期",
           "email": "收件人邮箱",
           "files_count": 读取的Markdown文件数量,
           "message": "成功/错误消息",
           "error": "错误信息（如果失败）"
         }
       
       实现要点：
       1. 验证日期格式（YYYY-MM-DD）
       2. 构建日期文件夹路径：output/YYYY-MM-DD/
       3. 检查文件夹是否存在
       4. 读取文件夹下所有.md文件（排除articles_cache.json等）
       5. 按文件名排序，确保顺序一致
       6. 解析每个Markdown文件，提取：
          - 原名（**原名**后面的内容）
          - AI总结（**ai总结内容**后面的内容）
          - 原文链接（**原文链接**后面的URL或Markdown链接）
       7. 生成HTML邮件正文：
          - 使用HTML模板格式化内容
          - 包含日期、文章列表、原文链接
          - 支持基本的HTML样式（标题、段落、链接等）
       8. 从配置文件读取SMTP设置：
          - SMTP服务器地址
          - SMTP端口
          - 发件人邮箱
          - 发件人密码或授权码
          - 是否使用TLS/SSL
       9. 使用smtplib发送邮件：
          - 建立SMTP连接
          - 登录邮箱账户
          - 发送邮件
          - 关闭连接
       10. 错误处理：
           - 文件夹不存在
           - 没有找到.md文件
           - Markdown解析失败
           - SMTP连接失败
           - 邮件发送失败
       
       邮件配置示例（config.yaml）：
       ```yaml
       email:
         smtp_server: "smtp.example.com"  # SMTP服务器地址
         smtp_port: 587  # SMTP端口（587为TLS，465为SSL）
         sender_email: "sender@example.com"  # 发件人邮箱
         sender_password: "your_password_or_app_password"  # 发件人密码或授权码
         use_tls: true  # 是否使用TLS（true使用TLS，false使用SSL）
         default_subject_prefix: "NewsFlow"  # 默认邮件主题前缀
       ```
       
       安全注意事项：
       - 密码或授权码应妥善保管，不要提交到版本控制系统
       - 建议使用应用专用密码（App Password）而非主密码
       - 支持通过环境变量读取敏感配置
       - 邮件发送失败时记录详细日志，但不暴露密码信息
       ```
   ```

**资源 (Resources)**：
1. **config**：读取配置文件（可选）
   ```python
   def read_config() -> dict:
       """读取config.yaml配置，返回配置字典"""
   ```

---

## 模块设计（MCP服务内部实现）

### 核心模块（Python实现，供MCP服务调用）

#### 1. ConfigManager（配置管理模块）
**功能**：读取和解析配置文件

**方法**：
- `load_config(config_path: str) -> dict`: 读取配置文件
- `validate_config(config: dict) -> bool`: 验证配置格式
- `get_websites() -> List[dict]`: 获取网站列表

**配置结构**：
```python
{
    "websites": [
        {
            "name": "网站名称",
            "url": "https://example.com",
            "enabled": true
        }
    ],
    "output": {
        "base_dir": "./output",
        "date_format": "%Y-%m-%d"
    },
    "email": {
        "smtp_server": "smtp.example.com",
        "smtp_port": 587,
        "sender_email": "sender@example.com",
        "sender_password": "your_password",
        "use_tls": true,
        "default_subject_prefix": "NewsFlow"
    },
    "settings": {
        "max_articles_per_site": 10,
        "request_timeout": 30,
        "concurrent_limit": 5
    }
}
```

#### 2. WebsiteProcessor（网站处理器）
**功能**：访问网站、提取链接（链接识别由Cursor完成）

**方法**：
- `fetch_website_html(url: str) -> str`: 获取网站HTML内容
- `extract_links(html: str, base_url: str) -> List[dict]`: 提取所有链接及其上下文
- **注意**：链接识别和判断由Cursor的AI能力完成，不在MCP服务中实现

**链接数据结构**：
```python
{
    "href": "相对或绝对URL",
    "text": "链接文本",
    "url": "完整URL",
    "context": {
        "parent_tag": "div",
        "parent_class": "news-list",
        "surrounding_text": "周围文本"
    }
}
```

#### 3. URLCollector（URL收集器）
**功能**：汇总和去重URL

**方法**：
- `collect_urls(urls_list: List[List[str]]) -> List[str]`: 汇总所有URL
- `deduplicate_urls(urls: List[str]) -> List[str]`: URL去重

#### 4. FileManager（文件管理模块）
**功能**：创建日期文件夹、保存Markdown文档

**方法**：
- `create_date_folder(date: str) -> str`: 创建日期文件夹，返回路径
- `sanitize_filename(title: str) -> str`: 文件名安全处理
- `save_markdown(file_path: str, content: dict) -> bool`: 保存Markdown文档

**Markdown内容结构**：
```python
{
    "original_title": "文章标题（格式：中文标题（英文原标题））",
    "summary": "AI生成的约200字简短摘要",
    "detailed_summary": "AI生成的500-800字详细概括（必需，不能为空）",
    "url": "文章原始URL"
}
```

### 文章处理模块

#### 5. ArticleProcessor（文章处理器）
**功能**：串行处理URL数组

**方法**：
- `process_urls(urls: List[str]) -> List[dict]`: 串行处理所有URL，返回Markdown文档列表
- `process_single_url(url: str) -> dict`: 处理单个URL，获取文章、AI分析、生成Markdown

#### 6. ArticleFetcher（文章获取模块）
**功能**：获取文章内容

**方法**：
- `fetch_article(url: str) -> dict`: 获取文章标题和正文

**返回结构**：
```python
{
    "url": "文章URL",
    "title": "文章标题",
    "content": "文章正文（纯文本）"
}
```

#### 7. AIAnalyzer（AI分析模块）
**功能**：调用AI生成文章总结

**注意**：此模块不需要实现，文章总结由Cursor的AI能力完成。Cursor读取文章内容后，使用自身AI能力生成一两百字总结，不需要调用外部AI API。

#### 8. MarkdownGenerator（Markdown生成器）
**功能**：生成格式化的Markdown文档

**方法**：
- `generate_markdown(title: str, summary: str, detailed_summary: str, url: str) -> str`: 生成Markdown内容

**生成格式**：
```markdown
**原名**
文章标题（格式：中文标题（英文原标题））

**ai总结内容**
AI生成的约200字简短摘要，采用轻松、口语化的表达方式

**详细概括**
AI生成的500-800字详细文章概括，包含以下结构化的部分：
- **背景信息**：简要介绍文章主题和背景
- **核心要点**：列出2-5个主要观点
- **关键信息**：重要的数据、事实、发现或具体案例
- **影响分析**：这个内容可能带来的影响、意义或启示
- **结论**：文章的核心结论或主要观点总结

**原文链接**
[文章原标题](文章原始URL)
```

**注意**：`detailed_summary` 字段是必需的，必须生成，不能为空。

#### 9. EmailSender（邮件发送模块）
**功能**：读取日期文件夹中的Markdown文件，发送邮件

**方法**：
- `read_markdown_files_from_folder(folder_path: str) -> List[dict]`: 读取文件夹中的所有Markdown文件并解析
- `parse_markdown_file(file_path: str) -> dict`: 解析单个Markdown文件，提取文章信息
- `generate_email_html(articles: List[dict], date: str) -> str`: 生成HTML格式的邮件正文
- `send_email(recipient: str, subject: str, html_content: str, config: dict) -> bool`: 通过SMTP发送邮件

**文章数据结构**：
```python
{
    "original_title": "文章标题（格式：中文标题（英文原标题））",
    "summary": "AI生成的约200字简短摘要",
    "detailed_summary": "AI生成的500-800字详细概括（可选）",
    "url": "文章原始URL",
    "filename": "文件名.md"
}
```

**邮件HTML模板结构**：
```html
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        /* 基本样式 */
    </style>
</head>
<body>
    <h1>NewsFlow - {date} 新闻摘要</h1>
    <p>共 {count} 篇文章</p>
    <hr>
    <!-- 文章列表 -->
    <div class="article">
        <h2>文章原标题</h2>
        <p class="summary">AI简短摘要（约200字）</p>
        <!-- 详细概括部分，默认折叠 -->
        <input type="checkbox" id="detail-{index}" class="detail-toggle" />
        <label for="detail-{index}" class="detail-label">查看详细概括</label>
        <div class="detail-content">AI详细概括（500-800字）</div>
        <p><a href="原文链接">阅读原文</a></p>
    </div>
    <!-- ... 更多文章 ... -->
</body>
</html>
```

### 核心业务模块

**注意**：AI相关功能（链接识别、文章总结）都由Cursor完成，不需要额外的AI服务模块：
- **链接识别**：Cursor直接读取链接内容并判断是否为文章链接
- **文章总结**：Cursor读取文章内容并生成一两百字总结
- **无需外部AI服务**：不依赖OpenAI、Claude等外部API

## 数据模型

### 配置文件结构

**config.yaml**：
```yaml
websites:
  - name: "示例新闻网站1"
    url: "https://news.example1.com"
    enabled: true
  - name: "示例新闻网站2"
    url: "https://news.example2.com"
    enabled: true

# 输出配置
output:
  base_dir: "./output"  # 输出基础目录
  date_format: "%Y-%m-%d"  # 日期格式

# 邮件配置
email:
  smtp_server: "smtp.gmail.com"  # SMTP服务器地址（示例：Gmail）
  smtp_port: 587  # SMTP端口（587为TLS，465为SSL）
  sender_email: "your_email@gmail.com"  # 发件人邮箱
  sender_password: "your_app_password"  # 发件人密码或授权码（建议使用应用专用密码）
  use_tls: true  # 是否使用TLS（true使用TLS/STARTTLS，false使用SSL）
  default_subject_prefix: "NewsFlow"  # 默认邮件主题前缀
  
# 其他配置
settings:
  max_articles_per_site: 10  # 每个网站最多识别文章数
  request_timeout: 30  # 请求超时时间（秒）
  retry_times: 3  # 重试次数
  # concurrent_limit: 5  # 并行处理并发数（后续版本使用）
```

**邮件配置说明**：
- **smtp_server**：根据邮箱服务商设置
  - Gmail: `smtp.gmail.com`
  - Outlook: `smtp-mail.outlook.com`
  - 163邮箱: `smtp.163.com`
  - QQ邮箱: `smtp.qq.com`
- **smtp_port**：
  - TLS: 587（推荐，使用STARTTLS）
  - SSL: 465（使用SSL/TLS加密）
- **sender_password**：
  - Gmail需要使用应用专用密码（App Password），而非账户密码
  - 其他邮箱服务商可能需要开启SMTP服务并设置授权码
- **use_tls**：true表示使用STARTTLS（端口587），false表示使用SSL（端口465）

### 数据流转

```
配置文件 → 网站列表
    ↓
网站URL → 网页HTML → 链接列表
    ↓
链接列表 → Cursor AI识别 → 新闻链接列表
    ↓
所有网站新闻链接 → URL去重 → URL数组
    ↓
URL数组 → 串行处理（逐个URL）
    ├─> 获取文章内容
    ├─> AI分析生成总结
    └─> 生成Markdown文档
    ↓
Markdown文档列表 → 按日期归档 → 保存到文件系统
    ↓
（可选）日期文件夹 → 读取所有.md文件 → 解析文章内容
    ↓
文章列表 → 生成HTML邮件正文 → SMTP发送 → 收件人邮箱
```

## 接口设计

### MCP服务器实现

#### MCP服务1：newsflow-extract/server.py
```python
from mcp.server import Server
from mcp.server.stdio import stdio_server
from newsflow_extract import extract_links_from_url

app = Server("newsflow-extract")

@app.list_tools()
async def list_tools():
    return [
        {
            "name": "extract_links_from_url",
            "description": "从指定URL提取所有链接及其上下文信息",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "url": {"type": "string", "description": "网站URL"}
                },
                "required": ["url"]
            }
        },
        {
            "name": "get_website_html",
            "description": "获取网站的HTML内容",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "url": {"type": "string", "description": "网站URL"}
                },
                "required": ["url"]
            }
        }
    ]

@app.call_tool()
async def call_tool(name: str, arguments: dict):
    if name == "extract_links_from_url":
        url = arguments.get("url")
        return await extract_links_from_url(url)

if __name__ == "__main__":
    stdio_server(app).run()
```

#### MCP服务2：newsflow-writer/server.py
```python
from mcp.server import Server
from mcp.server.stdio import stdio_server
from newsflow_writer import (
    save_markdown_file, 
    create_date_folder,
    send_email_from_date_folder
)

app = Server("newsflow-writer")

@app.list_tools()
async def list_tools():
    return [
        {
            "name": "save_markdown_file",
            "description": "保存Markdown文件到日期文件夹，按照需求文档格式",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "title": {
                        "type": "string",
                        "description": "文件名（文章标题）"
                    },
                    "original_title": {
                        "type": "string",
                        "description": "文章原标题（Markdown中的原名）"
                    },
                    "summary": {
                        "type": "string",
                        "description": "AI生成的一两百字总结"
                    },
                    "url": {
                        "type": "string",
                        "description": "文章原始URL"
                    },
                    "date": {
                        "type": "string",
                        "description": "日期（YYYY-MM-DD格式），默认为今天"
                    }
                },
                "required": ["title", "original_title", "summary", "url"]
            }
        },
        {
            "name": "create_date_folder",
            "description": "创建日期文件夹",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "date": {
                        "type": "string",
                        "description": "日期（YYYY-MM-DD格式），默认为今天"
                    }
                }
            }
        },
        {
            "name": "send_email_from_date_folder",
            "description": "读取指定日期文件夹中的所有Markdown文件，并发送邮件到指定邮箱",
            "inputSchema": {
                "type": "object",
                "properties": {
                    "date": {
                        "type": "string",
                        "description": "日期（YYYY-MM-DD格式），要读取的日期文件夹"
                    },
                    "recipient_email": {
                        "type": "string",
                        "description": "收件人邮箱地址"
                    },
                    "subject": {
                        "type": "string",
                        "description": "邮件主题（可选），默认为'NewsFlow - {date} 新闻摘要'"
                    }
                },
                "required": ["date", "recipient_email"]
            }
        }
    ]

@app.call_tool()
async def call_tool(name: str, arguments: dict):
    if name == "save_markdown_file":
        title = arguments.get("title")
        original_title = arguments.get("original_title")
        summary = arguments.get("summary")
        url = arguments.get("url")
        date = arguments.get("date")
        return await save_markdown_file(
            title, original_title, summary, url, date
        )
    elif name == "create_date_folder":
        date = arguments.get("date")
        return await create_date_folder(date)
    elif name == "send_email_from_date_folder":
        date = arguments.get("date")
        recipient_email = arguments.get("recipient_email")
        subject = arguments.get("subject")
        return await send_email_from_date_folder(
            date, recipient_email, subject
        )

@app.list_resources()
async def list_resources():
    return [
        {
            "uri": "config://config.yaml",
            "name": "配置文件",
            "mimeType": "application/yaml",
            "description": "NewsFlow配置文件"
        }
    ]

@app.read_resource()
async def read_resource(uri: str):
    if uri == "config://config.yaml":
        # 读取config.yaml配置文件
        from shared.config_manager import load_config
        config = load_config("config.yaml")
        return config

if __name__ == "__main__":
    stdio_server(app).run()
```

### 错误处理

统一错误处理格式：
```python
class NewsFlowError(Exception):
    """基础异常类"""
    pass

class ConfigError(NewsFlowError):
    """配置错误"""
    pass

class NetworkError(NewsFlowError):
    """网络请求失败"""
    pass

class ParseError(NewsFlowError):
    """HTML解析失败"""
    pass

class MCPError(NewsFlowError):
    """MCP服务调用失败"""
    pass

class FileSaveError(NewsFlowError):
    """文件保存失败"""
    pass

class EmailSendError(NewsFlowError):
    """邮件发送失败"""
    pass

class SMTPConnectionError(EmailSendError):
    """SMTP连接失败"""
    pass

class EmailConfigError(NewsFlowError):
    """邮件配置错误"""
    pass
```

## 数据库设计

**注意**：Demo阶段不涉及数据库存储，所有数据在内存中处理。

### 可选扩展：数据持久化

如果未来需要记录历史文章、去重等功能，可考虑使用：

#### SQLite数据库（轻量级）

**articles表**：
```sql
CREATE TABLE articles (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    url TEXT UNIQUE NOT NULL,
    title TEXT,
    source_url TEXT,
    fetched_at TIMESTAMP,
    analyzed_at TIMESTAMP,
    summary TEXT,
    sent_at TIMESTAMP
);

CREATE INDEX idx_url ON articles(url);
CREATE INDEX idx_source_url ON articles(source_url);
CREATE INDEX idx_fetched_at ON articles(fetched_at);
```

**websites表**：
```sql
CREATE TABLE websites (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    url TEXT UNIQUE NOT NULL,
    enabled BOOLEAN DEFAULT 1,
    last_fetched_at TIMESTAMP
);
```

## 部署方案

### 开发环境

1. **安装依赖**：
```bash
pip install -r requirements.txt
```

**requirements.txt**：
```
mcp>=0.1.0  # MCP Python SDK
selenium>=4.0.0  # 浏览器自动化，使用Chrome WebDriver
beautifulsoup4>=4.12.0  # HTML解析库
pyyaml>=6.0  # YAML配置文件解析
webdriver-manager>=4.0.0  # ChromeDriver自动管理
```

**注意**：
- Chrome浏览器：系统需安装Google Chrome
- ChromeDriver：由 `webdriver-manager` 自动管理，首次运行时会自动下载对应版本，无需手动安装
- 无头模式：Selenium使用headless模式运行，不会打开浏览器窗口
- 邮件发送：使用Python内置的 `smtplib` 和 `email` 模块，无需额外安装依赖

2. **创建配置文件**：
```bash
cp config.yaml.example config.yaml
# 编辑config.yaml，配置网站列表
```

### 运行方式

**通过Cursor交互式执行：**

1. **启动MCP服务**（Cursor会自动启动，或手动测试）：
```bash
# 测试MCP服务
python -m mcp_servers.newsflow_extract.server
python -m mcp_servers.newsflow_writer.server
```

2. **在Cursor中使用**：
   - 在Cursor中配置MCP服务器（见代码结构部分）
   - 通过对话方式执行任务，例如：
     ```
     用户: "帮我运行NewsFlow，从配置的网站提取新闻并生成Markdown"
     
     Cursor会：
     1. 读取配置文件（可选，通过MCP resource）
     2. 调用 newsflow-extract::extract_links_from_url 提取每个网站的链接
     3. 对每个链接：
        a. Cursor直接访问链接URL读取内容
        b. Cursor使用AI判断：是否为文章链接？
        c. 如果是文章链接：
           - Cursor读取文章内容
           - Cursor生成一两百字总结
           - 调用 newsflow-writer::save_markdown_file 保存文件
        d. 如果不是文章链接：跳过
     4. 完成后输出统计信息
     ```

### 生产环境

**注意**：MCP方案主要适用于交互式使用，如需自动化执行，可以考虑：

1. **结合Python脚本**：
   - 实现一个Python脚本，内部调用MCP服务的函数（不通过MCP协议）
   - 这样可以复用代码逻辑，同时支持自动化

2. **或使用任务调度器调用Cursor CLI**：
   - 使用脚本通过Cursor CLI执行任务（如果Cursor支持）
   - 这种方式较复杂，不推荐

**推荐**：对于生产环境的定时任务，建议采用混合方案：
- MCP服务提供功能接口
- Python脚本直接调用这些功能（不通过MCP协议）
- 定时任务调用Python脚本

## 开发规范

### 代码结构

```
newsflow/
├── mcp_servers/           # MCP服务目录
│   ├── newsflow_extract/  # URL提取服务
│   │   ├── server.py      # MCP服务器入口
│   │   ├── __init__.py
│   │   └── extractor.py   # 链接提取逻辑（支持CSR）
│   └── newsflow_writer/   # Markdown写入服务
│       ├── server.py      # MCP服务器入口
│       ├── __init__.py
│       └── writer.py      # 文件写入逻辑
├── shared/                 # 共享模块
│   ├── config_manager.py  # 配置管理
│   ├── utils.py           # 工具函数
│   └── logger.py          # 日志
├── config.yaml            # 配置文件
├── config.yaml.example    # 配置文件模板
├── requirements.txt       # Python依赖
├── output/                # 输出目录（gitignore）
└── README.md              # 项目说明
```

### Cursor配置

在Cursor中配置MCP服务器（`.cursor/mcp.json` 或 Cursor设置中）：

```json
{
  "mcpServers": {
    "newsflow-extract": {
      "command": "python",
      "args": ["-m", "mcp_servers.newsflow_extract.server"],
      "cwd": "/path/to/newsflow"
    },
    "newsflow-writer": {
      "command": "python",
      "args": ["-m", "mcp_servers.newsflow_writer.server"],
      "cwd": "/path/to/newsflow"
    }
  }
}
```

### 代码规范

1. **命名规范**：
   - 函数/变量：`snake_case`
   - 类名：`PascalCase`
   - 常量：`UPPER_SNAKE_CASE`

2. **错误处理**：
   - 所有函数必须有try-catch
   - 使用自定义异常类
   - 记录详细日志

3. **日志规范**：
   - 使用Python logging模块
   - 包含关键信息：URL、参数、结果
   - 不记录敏感信息到日志
   - 日志级别：DEBUG, INFO, WARNING, ERROR

4. **类型提示**：
   - Python代码使用type hints
   - 函数参数和返回值明确类型
   - 使用`typing`模块的类型注解

5. **处理流程**（Demo阶段）：
   - 采用串行处理，逐个处理URL
   - 确保错误处理完善，单个失败不影响整体
   - 记录处理进度和日志
   - 后续版本可升级为并发处理

### 开发流程

1. **实现MCP服务基础设施**：
   - 搭建MCP服务器框架
   - 配置项目结构

2. **实现MCP服务1：URL提取服务**：
   - 实现extract_links_from_url工具
   - 集成Selenium Chrome WebDriver（按照代码示例）
   - 实现页面渲染等待（driver.implicitly_wait(5)）
   - 使用BeautifulSoup解析HTML并提取链接
   - 实现URL绝对路径转换
   - 测试MCP服务连接（需确保Chrome已安装，ChromeDriver会自动管理）

3. **实现MCP服务2：Markdown写入服务**：
   - 实现save_markdown_file工具（严格按照需求文档格式）
   - 实现create_date_folder工具
   - 实现文件名安全处理
   - 实现config资源（可选）
   - **实现邮件发送功能**：
     - 实现send_email_from_date_folder工具
     - 实现Markdown文件读取和解析（从日期文件夹读取所有.md文件）
     - 实现HTML邮件正文生成（格式化文章列表）
     - 实现SMTP邮件发送（支持TLS/SSL）
     - 添加邮件配置管理（从config.yaml读取SMTP设置）
     - 实现错误处理和日志记录

4. **在Cursor中配置和测试**：
   - 配置MCP服务器连接
   - 通过对话测试完整流程：
     - 测试链接提取（包括CSR网站）
     - 测试Cursor的链接判断和总结生成
     - 测试Markdown文件保存格式
     - 测试邮件发送功能：
       - 配置邮件SMTP设置（在config.yaml中）
       - 创建测试日期文件夹和Markdown文件
       - 调用send_email_from_date_folder工具
       - 验证邮件是否正确发送到指定邮箱
   - 验证所有功能

5. **优化和文档**：
   - 优化错误处理
   - 完善日志记录
   - 编写使用文档

## 附录

### Cursor AI任务提示

由于使用MCP方案，AI任务（链接识别和文章总结）由Cursor完成。以下是给Cursor的提示参考：

#### 链接识别任务提示
当Cursor需要识别新闻链接时，可以参考以下思路：

```
作为新闻链接识别专家，我需要分析以下链接列表，识别出新闻/咨询类文章链接。

分析要素：
1. 链接文本内容（如"今日要闻"是新闻，"用户协议"不是）
2. URL路径结构（通常新闻文章URL包含日期、文章ID等）
3. HTML上下文信息（链接所在区域、父元素标签）
4. 语义理解（判断链接指向的内容类型）

需要过滤的链接类型：
- 导航链接（首页、栏目页）
- 登录/注册链接
- 广告链接
- 用户协议、隐私政策
- 关于我们、联系方式

请只返回新闻文章链接的URL列表。
```

#### 文章总结任务提示
当Cursor需要生成文章总结时：

```
请分析以下新闻文章，生成简洁的总结（一两百字）。

要求：
1. 简洁明了，一两百字
2. 突出核心信息和要点
3. 使用中文
4. 直接返回总结内容（纯文本）
```

**注意**：这些提示会通过对话自然传达给Cursor，不需要硬编码在MCP服务中。

### 安全注意事项

1. **网络请求**：
   - 设置合理的User-Agent
   - 限制请求频率，避免被封禁
   - 使用HTTPS连接
   - 设置请求超时

2. **文件系统安全**：
   - 文件名进行安全处理，去除特殊字符
   - 限制文件名长度
   - 避免路径遍历攻击
   - 检查文件权限

3. **邮件发送安全**：
   - 邮箱密码或授权码应妥善保管，不要提交到版本控制系统（可使用环境变量）
   - Gmail等邮箱服务商建议使用应用专用密码（App Password），而非账户主密码
   - SMTP连接使用TLS/SSL加密传输
   - 邮件发送失败时记录详细日志，但不暴露密码等敏感信息
   - 建议将敏感配置（如密码）通过环境变量或密钥管理服务管理

### 性能优化建议

**Demo阶段（串行处理）**：
1. **基础优化**：
   - 确保请求超时设置合理，避免长时间等待
   - 单个网站或文章处理失败时快速跳过，不影响其他处理
   - 记录处理进度，便于监控和调试

2. **缓存策略**（可选）：
   - 缓存已处理的链接（避免Cursor重复判断）
   - 缓存文章内容（短时间内相同URL不重复获取）

3. **处理优化**：
   - Cursor会智能处理链接判断和总结生成
   - 无需担心API调用次数和成本（由Cursor内部处理）

4. **资源管理**：
   - 及时关闭文件句柄
   - 控制内存使用（大量URL时考虑分批处理）

**后续版本优化（并行处理）**：
1. **并发处理**：
   - 多个网站可并行处理（注意限流）
   - 多篇文章并行分析和生成Markdown（由Cursor协调）
   - 合理设置并发数（建议5-10个）

2. **异步IO**：
   - 使用`aiohttp`进行异步HTTP请求
   - 提高并发处理效率

