# NewsFlow Python依赖

# MCP框架（Model Context Protocol SDK）
# 注意：MCP SDK需要Python 3.10+
# 如果当前Python版本<3.10，请升级Python或使用: pip install mcp --no-deps（不推荐）
# 安装命令: pip install mcp[cli]
# 或使用官方包: pip install @modelcontextprotocol/server-python
mcp[cli]>=1.0.0

# 浏览器自动化（支持CSR网站）
selenium>=4.0.0

# HTML解析
beautifulsoup4>=4.12.0

# 配置文件解析
pyyaml>=6.0

# ChromeDriver自动管理
webdriver-manager>=4.0.0

# Markdown转HTML
markdown>=3.4.0

# HTML RAG检索工具
sentence-transformers>=2.2.0
numpy>=1.24.0

