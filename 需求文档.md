# 需求文档

## 项目概述

NewsFlow 是一个新闻聚合和智能摘要系统，旨在帮助用户自动收集多个新闻网站的最新文章，并通过AI分析生成摘要，以Markdown文档形式保存。

**系统形态**：
- 本系统是一个 **Python服务程序**
- 运行时会自动读取配置文件
- 通过Agent（AI）读取网站内容并分析识别新闻链接
- 使用并行服务处理新闻URL，生成Markdown文档

**核心价值**：
- 自动化收集多个新闻源的最新资讯
- **利用AI智能识别网页中的新闻链接**，自动过滤非新闻内容（无需为每个网站编写特定规则）
- 利用AI技术快速理解文章内容并生成摘要
- 以结构化的Markdown文档形式保存，便于阅读和存档

**技术亮点**：
- **AI驱动的链接识别**：通过语义理解自动区分新闻链接和导航、广告等非新闻链接
- **并行处理**：使用独立服务并行处理多个新闻URL，提高效率
- **日期归档**：按日期组织文档，便于管理和查找
- **通用性强**：适应不同网站的HTML结构，无需针对每个网站定制爬取规则

## 功能需求

### 功能点 1：配置自动读取

**需求描述**：
- 提供一个配置文件（如 JSON、YAML 或文本文件）用于维护要监控的新闻网站地址列表
- 程序启动时自动读取配置文件
- 支持添加、删除、修改网站地址
- 配置文件应支持多个网站地址的存储
- 每个网站地址应包含必要的元信息（如网站名称、RSS地址、爬取规则等，可选）
- 配置文件不存在或格式错误时，程序应提示错误并退出

**输入**：配置文件（程序启动时自动读取）
**输出**：网站地址列表（供后续处理使用）

### 功能点 2：网站内容抓取与AI链接识别

**需求描述**：
- 程序运行时，自动访问配置文件中维护的每个网站
- 使用Agent（AI）读取网站的最新网页内容
- 从网页HTML中提取所有链接（a标签的href属性）及其上下文信息
- **AI智能链接识别**（核心功能）：
  - 将提取到的所有链接及其上下文信息（链接文本、URL、父元素、相邻文本等）交给Agent分析
  - Agent需要判断每个链接是否为新闻/咨询类文章链接
  - Agent分析要素包括：
    - 链接文本内容（如"今日要闻"、"用户协议"等）
    - URL模式（路径结构、参数等）
    - HTML上下文（链接所在的区域、父元素标签等）
    - 语义理解（判断链接指向的内容类型）
  - Agent返回结果应包含：是否为新闻链接（布尔值）、置信度（可选）
  - 过滤掉非新闻链接（如：登录页、用户协议、关于我们、广告链接、导航链接等）
- 只保留Agent识别为新闻/咨询类的链接
- 对新闻链接进行去重（合并重复的URL）
- 最终汇总成一个URL数组，传递给后续处理服务

**AI链接识别的优势**：
- 无需为每个网站编写特定的爬取规则
- 能够适应不同网站的结构差异
- 通过语义理解准确区分新闻链接和非新闻链接
- 减少人工维护成本，提高系统通用性

**输入**：网站地址列表（从配置文件读取）
**输出**：新闻文章URL数组（去重后的链接列表）

### 功能点 3：并行AI文章分析与Markdown生成

**需求描述**：
- 接收新闻URL数组
- **使用另一个独立服务**并行处理所有URL（提高效率）
- 对每个URL执行：
  1. 访问文章链接，获取文章正文内容
  2. 调用AI接口（如OpenAI API、Claude API等）分析文章内容并生成总结
  3. 生成Markdown文档
- AI分析内容应包括：
  - 文章标题（原文标题）
  - 文章总结（AI生成的摘要内容）
- 处理失败的文章应记录日志，不影响其他文章的处理

**Markdown文档结构**：
- 文件名为：`文章标题.md`（格式：中文标题（英文原标题），需要进行文件名安全处理，如去除特殊字符）
- 文档内容格式：
  ```markdown
  **原名**
  文章标题（格式：中文标题（英文原标题））
  
  **ai总结内容**
  AI生成的约200字简短摘要，采用轻松、口语化的表达方式，包含文章核心观点、关键信息和主要结论。语气自然放松，像朋友聊天一样娓娓道来，在合适的位置适当添加表情符号，让总结更生动有趣。
  
  **详细概括**
  AI生成的500-800字详细文章概括，包含以下结构化的部分：
  - **背景信息**：简要介绍文章主题和背景
  - **核心要点**：列出2-5个主要观点
  - **关键信息**：重要的数据、事实、发现或具体案例
  - **影响分析**：这个内容可能带来的影响、意义或启示
  - **结论**：文章的核心结论或主要观点总结
  保持轻松、口语化的风格，但内容要更加详细和全面，确保是对文章的深入分析。
  
  **原文链接**
  [文章原标题](文章原始URL)
  ```

**注意**：`详细概括` 字段是必需的，必须生成，不能为空。

**输入**：新闻URL数组
**输出**：Markdown文档列表（每篇文章对应一个MD文件）

### 功能点 4：按日期归档Markdown文档

**需求描述**：
- 接收生成的Markdown文档列表
- 创建以日期命名的文件夹（格式：`YYYY-MM-DD`，如 `2024-01-01`）
- 将当天处理的所有Markdown文档保存到该日期文件夹下
- 如果该日期文件夹已存在，直接在其中添加新文件
- 如果文件已存在（同名），可以选择覆盖或跳过（建议覆盖，以获取最新内容）

**文件组织结构**：
```
output/
├── 2024-01-01/
│   ├── 文章标题1.md
│   ├── 文章标题2.md
│   └── 文章标题3.md
├── 2024-01-02/
│   ├── 文章标题1.md
│   └── 文章标题2.md
└── ...
```

**输入**：Markdown文档列表（包含文件名和内容）
**输出**：文件保存成功/失败状态，保存路径信息

## 非功能需求

### 性能要求
- 单个网站的内容抓取和链接识别应在30秒内完成
- 单篇文章的AI分析和Markdown生成应在10秒内完成（取决于AI API响应速度）
- 并行处理服务应支持同时处理多个URL（建议并发数：5-10个）
- 整体流程应在合理时间内完成（取决于网站数量和文章数量）

### 安全要求
- 配置文件中的敏感信息（如API密钥）应使用环境变量或加密存储
- 访问网站时应设置合理的请求头，避免被封禁
- 文件名应进行安全处理，去除或替换特殊字符，避免文件系统安全问题

### 兼容性要求
- 支持主流操作系统（Windows、macOS、Linux）
- 支持Python 3.8+（假设使用Python实现）
- 能够处理不同网站的HTML结构差异

### 可靠性要求
- 单个网站抓取失败不应影响其他网站的处理
- 单篇文章分析失败应记录日志，继续处理其他文章
- 提供基本的错误处理和日志记录

## 用户故事

**作为用户**，我希望：
1. 能够通过配置文件轻松添加我想关注的新闻网站
2. 运行一次程序就能自动获取所有网站的最新文章
3. 所有文章以Markdown文档形式保存，按日期组织，便于查看和归档
4. 每个Markdown文档包含文章原名、AI总结和原文链接，信息完整
5. 如果某个网站或文章处理失败，程序能够继续处理其他内容并告知我

## 验收标准

### 功能验收
- [ ] 程序启动时能够自动读取配置文件并获取网站地址列表
- [ ] 能够成功访问配置的网站并使用Agent分析网页内容
- [ ] **能够使用Agent准确识别新闻/咨询链接，过滤掉非新闻链接（如导航、登录、广告等）**
- [ ] **Agent链接识别的准确率应达到85%以上（正确识别为新闻链接的链接数 / 实际新闻链接数）**
- [ ] 能够将识别出的新闻链接汇总成URL数组
- [ ] 能够使用并行服务处理URL数组，获取文章内容并生成AI总结
- [ ] 能够生成符合格式要求的Markdown文档（包含原名、AI总结、原文链接）
- [ ] 能够按日期创建文件夹并将Markdown文档保存到对应文件夹
- [ ] 文件名使用文章标题，并进行安全处理（去除特殊字符）

### 异常处理验收
- [ ] 配置文件不存在或格式错误时，程序能够提示错误并退出
- [ ] 网站无法访问时，程序能够记录错误并继续处理其他网站
- [ ] Agent API调用失败时，程序能够记录错误并继续处理其他网站
- [ ] 文章内容获取失败时，程序能够记录错误并继续处理其他文章
- [ ] AI分析失败时，程序能够记录错误并继续处理其他文章
- [ ] 文件保存失败时，程序能够记录错误信息

### 日志验收
- [ ] 程序运行过程中能够输出基本的日志信息
- [ ] 日志应包含：开始时间、处理的网站数量、抓取的文章数量、成功/失败统计

## 待定事项

1. **Agent选择**：使用哪个AI服务作为Agent？（OpenAI、Claude、国产AI等）- **需要支持网站内容读取和链接识别**
2. **并行处理服务**：
   - 是使用另一个独立的Python服务还是同一程序内的并行模块？
   - 并行处理的并发数设置（建议5-10个）
   - 是否需要使用消息队列（如Redis）解耦两个服务？
3. **网站抓取方式**：是否需要支持RSS订阅？还是只支持HTML爬取？（demo阶段先支持HTML爬取）
4. **AI链接识别实现**：
   - 是否需要将链接批量发送给Agent（提高效率），还是逐个发送？
   - Agent识别的Prompt设计：如何描述任务让Agent更准确地识别新闻链接？
   - 是否需要缓存Agent识别结果以提高性能？
5. **去重机制**：URL去重的具体策略（精确匹配还是模糊匹配？）
6. **AI服务选择**：用于文章内容分析的AI服务（OpenAI、Claude、国产AI等）- 可以与Agent使用不同的服务
7. **运行方式**：手动运行还是定时任务？（demo阶段先实现手动运行）
8. **文章数量限制**：每个网站识别多少篇最新文章？是否需要限制总数？
9. **链接上下文提取**：提取链接的上下文信息时，需要包含多少HTML结构信息？
10. **文件名处理**：
    - 文件名的最大长度限制
    - 特殊字符的处理策略（替换为下划线或移除）
    - 重名文件的处理（添加序号？）
11. **输出目录**：Markdown文档保存的基础目录路径配置
12. **日期时区**：使用哪个时区确定"今天"的日期？（建议使用本地时区）

